<?xml version="1.0" encoding="utf-8"?>
<!-- LAN hub heartbeats use HTTP on the local subnet (no TLS on mesh LAN). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
