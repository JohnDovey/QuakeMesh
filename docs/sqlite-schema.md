# SQLite Schema Reference

Placeholder. See "Storage - SQLite Everywhere" in [/plan.md](../plan.md)
for the full table list (`node_registry`, `routing_table`,
`proximity_events`, `trust_endorsements`, `hub_registry`, `relay_hubs`,
`app_presence`, `ban_list`, `dtq_queue`, `historical_metrics`,
`admin_users`, `config`). This file will hold the generated schema
reference once migrations land in `/core/storage` (Phase 1).
