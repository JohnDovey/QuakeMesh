# Monitor API Spec

Placeholder. See "QuakeMeshMonitor" in [/plan.md](../plan.md) for the
Hub loopback management API (`127.0.0.1:8083`) and the Monitor's `/ws`
WebSocket event stream. This file will hold the versioned API spec once
`/hub`'s management API and `/monitor`'s WebSocket consumer land
(Phase 2/3).
